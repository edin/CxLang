# CX Language Support

This extension provides syntax highlighting and live compiler diagnostics for `.cx` files.

Install the CX CLI so `cx` is available on `PATH`, or set `cx.languageServer.path` to the executable path. The extension starts the language server with `cx lsp`.

For extension development, open this directory in VS Code, run `npm install`, and launch an Extension Development Host.
