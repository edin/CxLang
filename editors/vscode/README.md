# CX Language Support

This extension provides syntax highlighting, live compiler diagnostics, and member completion for `.cx` files.

Packaged builds include a matching CX language server. Set `cx.languageServer.path` only to override the bundled server.

For extension development, open this directory in VS Code, run `npm install`, and launch an Extension Development Host.
